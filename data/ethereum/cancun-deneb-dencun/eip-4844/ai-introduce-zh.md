# EIP-4844: Proto-Danksharding / Blob 交易 — AI 解读

## 一句话总结

> 以太坊扩容路线图的关键里程碑。Blob 为 Rollup 提供了廉价的数据可用性——每个 blob 约 128KB 数据仅需约 1-10 gwei/字节，比 calldata 便宜 100 倍。KZG 承诺允许 EVM 在不读取完整 blob 的情况下验证数据正确性。这不是完整的 Danksharding，而是'原型'版本，已经让 L2 费用降低 90%+。

## 核心机制拆解

### 1. 引入 Blob 携带交易（Blob Transaction）

**技术原文**: 引入 Blob 携带交易（Blob Transaction）：

**通俗理解**: 一种临时的数据存储方式，专门给 Rollup 用的，便宜但会定期删除。

### 2. 新交易类型包含最多 6 个 blob（每个 128KB）

**技术原文**: 新交易类型包含最多 6 个 blob（每个 128KB）

**通俗理解**: 一种临时的数据存储方式，专门给 Rollup 用的，便宜但会定期删除。

### 3. Blob 数据使用 KZG 多项式承诺

**技术原文**: Blob 数据使用 KZG 多项式承诺

**通俗理解**: 一种临时的数据存储方式，专门给 Rollup 用的，便宜但会定期删除。

### 4. Blob 在共识层短期保存（约 4096 个 epoch ~ 18 天）后删除

**技术原文**: Blob 在共识层短期保存（约 4096 个 epoch ~ 18 天）后删除

**通俗理解**: 一种临时的数据存储方式，专门给 Rollup 用的，便宜但会定期删除。

### 5. 独立的 blob gas 市场和基础费

**技术原文**: 独立的 blob gas 市场和基础费

**通俗理解**: 这是关于交易费用的调整，影响你发送交易时需要支付的手续费。

### 6. EVM 可通过 POINTEVALUATION 预编译验证 KZG 证明。

**技术原文**: EVM 可通过 POINTEVALUATION 预编译验证 KZG 证明。

**通俗理解**: 一种数学证明技术，用来确保别人没篡改你提交的数据。

## 实际影响分析

🔴 **高影响** — 此变更对以太坊生态产生深远影响，建议所有开发者和用户关注。

**涉及领域**: 扩容/L2

### 谁会受到影响？

- **普通用户**: 交易费用可能发生变化，需要关注 gas 成本波动。
- **智能合约开发者**: 可能需要更新合约代码以适应新机制或利用新功能。
- **Layer 2 / Rollup 开发者**: 直接受益于更低的链上数据成本或更高的吞吐量。

## 历史背景与演进

EIP-4844 是"The Surge"扩容路线图的核心。它引入了 Proto-Danksharding 概念，为完整的 Danksharding 铺路。Blob 交易让 Rollup 的数据成本降低了 10-100 倍。

---

*本解读由 AI 基于技术文档生成，仅供参考。具体实现细节请以官方 EIP 为准。*