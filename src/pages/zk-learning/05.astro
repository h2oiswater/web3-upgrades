---
import Layout from '../../layouts/Layout.astro';
const baseUrl = import.meta.env.BASE_URL;
const htmlContent = `<h1>5. 数学基础IV：多项式、插值与FFT——ZK证明的核心语言</h1>
<hr>
<h2>学习目标</h2>
<p>在本章结束时，你应该能够：</p>
<ul><li><strong>解释多项式在ZK中的核心地位</strong>：理解为什么SNARK和STARK都将计算约束编码为多项式等式，以及多项式的唯一性如何支撑"单点验证=全局确认"的魔法。</li>
<li><strong>掌握拉格朗日插值（Lagrange Interpolation）</strong>：从一组点 \$(x_i, y_i)\$ 重构唯一的多项式 \$p(x)\$，理解基多项式（basis polynomial）的直觉，并能手写或编码实现。</li>
<li><strong>理解FFT如何加速ZK中的多项式运算</strong>：从 \$O(n^2)\$ 的朴素多项式乘法到 \$O(n \\log n)\$ 的FFT加速，理解分治策略中的蝶形运算（butterfly operation），以及数论变换（NTT）在有限域上的适配。</li>
<li><strong>建立多项式承诺（Polynomial Commitment）的直觉</strong>：理解"先封印多项式，后揭示特定点"这一模式，为后续学习KZG承诺（第7-8章）打下直觉基础。</li>
</ul><hr>
<h2>5.1 为什么ZK选择了多项式</h2>
<p>在进入形式化定义之前，让我们先解决一个关键问题：<strong>为什么现代ZK证明系统——从Groth16到PLONK到STARK——都把多项式当作核心语言？</strong></p>
<h3>5.1.1 类比：多项式像一种"压缩的指纹"</h3>
<p>你可以把多项式想象成一个<strong>无限容量的信息容器</strong>——只用少量系数，就能编码无限多个约束。具体地说：</p>
<ul><li>一个次数为 \$d\$ 的多项式 \$p(x) = a_0 + a_1x + a_2x^2 + \\cdots + a_dx^d\$ 只有 \$d+1\$ 个系数；</li>
<li>但这 \$d+1\$ 个系数唯一决定了该多项式在<strong>无穷多个点</strong>上的取值。</li>
</ul><p>这就像你有一把钥匙（少量系数），能打开无数把锁（无数个点的值）——但锁与锁之间并非独立，它们被同一套代数规则牢牢绑定。Vitalik Buterin在介绍zk-SNARKs时特别强调，所有运算不是在常规实数上进行的，而是在有限域 \$F_p\$ 上进行的——一种自洽的算术，所有答案都落在某个有限集合中[^274^]。而多项式，正是在有限域上编码信息的最优雅工具。</p>
<h3>5.1.2 核心洞察：验证大量约束 = 验证多项式在特定点取值为零</h3>
<p>这是所有现代ZK系统的核心魔法，理解它你就理解了一半的ZK：</p>
<blockquote><strong>如果一个多项式 \$P(x)\$ 在 \$n\$ 个不同点上都等于零，那么 \$P(x)\$ 一定能被 \$(x-r_1)(x-r_2)\\cdots(x-r_n)\$ 整除。</strong></blockquote>
<p>在ZK中，我们把<strong>电路的所有约束</strong>编码成这样一个"约束多项式"——如果计算正确，它在所有约束点处都为零。于是，验证整个电路的正确性，等价于验证这个多项式能否被某个已知的"消失多项式"（vanishing polynomial）整除[^108^]。</p>
<p>这种"从约束到多项式"的转换，正是下一章要讲的QAP（Quadratic Arithmetic Program）和PLONK的核心步骤。你可以把QAP看作一台翻译机：左边输入"电路的约束列表"，右边输出"一组多项式恒等式"[^149^]。</p>
<h3>5.1.3 多项式的三大优势</h3>
<p>| 优势 | 含义 | 在ZK中的体现 |<br>|:---:|:---|:---|<br>| <strong>代数结构丰富</strong> | 多项式环 \$F[x]\$ 支持加减乘除（模不可约多项式），运算封闭且可逆 | 约束可以被组合、分解、缩放而不破坏结构 |<br>| <strong>插值唯一性</strong> | \$n+1\$ 个点唯一确定一个次数不超过 \$n\$ 的多项式 | 电路witness可被唯一编码为多项式，防止篡改 |<br>| <strong>易于承诺</strong> | 可用椭圆曲线点"压缩"整个多项式（如KZG） | 证明者只需发送一个短承诺，而非全部系数 |</p>
<p>正是这三个特性的结合，使得多项式成为ZK证明的<strong>通用语言</strong>——无论你用Groth16、PLONK还是STARK，底层都在说"多项式话"。</p>
<hr>
<h2>5.2 多项式基础运算</h2>
<h3>5.2.1 系数视角下的加减乘</h3>
<p>一个次数不超过 \$n\$ 的多项式可以用系数向量表示：</p>
<p>\$\$p(x) = a_0 + a_1x + a_2x^2 + \\cdots + a_nx^n \\quad \\leftrightarrow \\quad \\mathbf{a} = [a_0, a_1, \\ldots, a_n]\$\$</p>
<p>在有限域 \$F_p\$ 上，系数和变量的所有运算都遵循模 \$p\$ 规则：</p>
<ul><li><strong>加法</strong>：对应系数相加后取模，\$\\mathbf{a} + \\mathbf{b} = [(a_0+b_0) \\bmod p, \\ldots]\$</li>
<li><strong>减法</strong>：加法逆元，\$\\mathbf{a} - \\mathbf{b} = [(a_0-b_0) \\bmod p, \\ldots]\$</li>
<li><strong>乘法</strong>（卷积）：\$(a \\star b)_k = \\sum_{i+j=k} a_i \\cdot b_j \\pmod p\$，这是复杂度为 \$O(n^2)\$ 的操作</li>
</ul><p>乘法是其中最昂贵的运算，也是FFT将要解决的核心瓶颈。</p>
<h3>5.2.2 根与因式分解</h3>
<p>如果 \$p(r) = 0\$，我们称 \$r\$ 是多项式 \$p(x)\$ 的一个<strong>根</strong>（root）。代数基本定理告诉我们：</p>
<blockquote><strong>在域上，一个 \$d\$ 次多项式最多有 \$d\$ 个根。</strong></blockquote>
<p>这意味着：如果两个不同的 \$d\$ 次多项式 \$P(x)\$ 和 \$Q(x)\$ 相等，它们必须在<strong>几乎所有点</strong>上相等；而如果有差异，它们最多只在 \$d\$ 个点上"恰好碰撞"。这正是<strong>Schwartz-Zippel引理</strong>的直觉：</p>
<blockquote>两个不同 \$d\$ 次多项式在随机点处取值相等的概率不超过 \$d / |F_p|\$。在密码学尺度下（\$p\$ 是256位大素数），这个概率小到可以忽略[^194^]。</blockquote>
<p>这就是为什么"验证单点"就能以极高置信度确认整个多项式——就像DNA鉴定，两个不同个体的DNA恰好匹配的概率微乎其微。</p>
<h3>5.2.3 次数（Degree）：ZK中"复杂度"的度量</h3>
<p>多项式的<strong>次数</strong>（degree，记作 \$\\deg(p)\$）是最高非零项的指数。在ZK中：</p>
<ul><li>次数 \$</li></ul>
ightarrow\$ 证明的大小和计算量
<ul><li>乘法门数量 \$</li></ul>
ightarrow\$ 电路的约束数 \$
ightarrow\$ 多项式的次数
<ul><li>FFT的加速效果 \$</li></ul>
ightarrow\$ 次数越高越显著（\$O(n^2)\$ 与 \$O(n \\log n)\$ 的差距随 \$n\$ 增大）
<p>在STARK中，执行轨迹被编码为多项式后，证明系统的核心任务之一就是证明"这个多项式的次数没有超过声明值"——这通过FRI（Fast Reed-Solomon IOP）递归折叠来实现[^112^]。</p>
<hr>
<h2>5.3 拉格朗日插值：从点集重构多项式</h2>
<h3>5.3.1 类比：用几根柱子重建整个栅栏的形状</h3>
<p>想象你看到一个波浪形的木栅栏，但只能走近观察其中的几根柱子。插值问题就是：<strong>能否仅凭这几根柱子的高度，推断出整条栅栏的完整曲线？</strong></p>
<p>答案是——如果这些柱子足够多且位置合适，你<strong>唯一地</strong>确定了这条曲线。对于多项式来说，给定 \$n+1\$ 个不同横坐标上的点，存在<strong>唯一一个</strong>次数不超过 \$n\$ 的多项式穿过它们。</p>
<h3>5.3.2 拉格朗日基多项式：每个点"举手表决"</h3>
<p>拉格朗日插值的核心构造是<strong>基多项式</strong>（Lagrange basis polynomial）。想象每个点都"举手"说："我只在我这个 \$x\$ 处起作用，其他地方都是0。"[^256^]</p>
<p>对于 \$n+1\$ 个点 \$(x_0, y_0), (x_1, y_1), \\ldots, (x_n, y_n)\$，第 \$i\$ 个基多项式定义为：</p>
<p>\$\$L_i(x) = \\prod_{j \\neq i} \\frac{x - x_j}{x_i - x_j}\$\$</p>
<p>它的构造保证了两个关键性质：<br><ul><li>\$L_i(x_i) = 1\$（在自己的"主场"取值为1）</li><br><li>\$L_i(x_j) = 0\$（在所有其他人的"主场"取值为0）</li></ul></p>
<p>最终的插值多项式就是所有基多项式的加权叠加：</p>
<p>\$\$p(x) = \\sum_{i=0}^{n} y_i \\cdot L_i(x)\$\$</p>
<h3>5.3.3 拉格朗日插值的可视化</h3>
<pre class="code-block"><code>拉格朗日插值：用基多项式叠加重构目标曲线

点集: (x₀,y₀), (x₁,y₁), (x₂,y₂), (x₃,y₃)

基多项式 L₀(x) ──┐
    =1 在 x=x₀    │   L₀(x)·y₀
    =0 在 x≠x₀    │      ▲
                  │      │   ╱╲
                  │   ───┼──╱──╲──────
                  │      │ ╱      ╲
                  │      │╱          ╲
基多项式 L₁(x) ──┤
    =1 在 x=x₁    │   L₁(x)·y₁
    =0 在 x≠x₁    │         ▲
                  │    ─────┼────╱╲───
                  │         │   ╱    ╲
基多项式 L₂(x) ──┤
    =1 在 x=x₂    │   L₂(x)·y₂
    =0 在 x≠x₂    │              ▲
                  │    ──────────┼──╱╲──
                  │              │ ╱    ╲
基多项式 L₃(x) ──┤
    =1 在 x=x₃    │   L₃(x)·y₃
    =0 在 x≠x₃    │                   ▲
                  │    ──────────────┼──╱╲
                  │                  │ ╱
                  ▼                  ▼
───────────────────────────────────────────
        ↓ 叠加求和 ↓
                  
最终插值多项式 p(x) = y₀·L₀(x) + y₁·L₁(x) + y₂·L₂(x) + y₃·L₃(x)

    p(x)
    ▲
    │  ╱╲    ╱╲
    │ ╱  ╲  ╱  ╲    ← 重构的曲线穿过所有给定点
    │╱    ╲╱    ╲
    └──────────────────────► x
      x₀  x₁  x₂  x₃
</code></pre>
<h3>5.3.4 在ZK中的应用：从电路约束点到多项式</h3>
<p>拉格朗日插值在ZK中最关键的应用是<strong>QAP构造</strong>：R1CS的每一组约束向量被看作一个"列"，该列在约束点 \$x=1, 2, \\ldots, n\$ 处的取值就是向量元素。通过拉格朗日插值，每一列变成一个多项式[^64^]。</p>
<p>例如，如果某列在4个约束点上的值为 \$[c_1, c_2, c_3, c_4]\$，我们构造多项式 \$C(x)\$ 使得：</p>
<p>\$\$C(1)=c_1, \\quad C(2)=c_2, \\quad C(3)=c_3, \\quad C(4)=c_4\$\$</p>
<p>这样，验证"所有约束同时满足"就转化为验证"多项式等式 \$A(x)\\cdot B(x) - C(x) = H(x)\\cdot Z(x)\$ 在所有约束点处成立"——而 \$Z(x) = (x-1)(x-2)(x-3)(x-4)\$ 恰好在这些点处为零[^108^]。</p>
<p>PLONK同样大量使用拉格朗日插值：门约束（gate constraints）和拷贝约束（copy constraints）都被编码为多项式等式，利用插值将离散的电路表格转化为连续的代数对象[^27^]。</p>
<hr>
<h2>5.4 快速傅里叶变换（FFT）</h2>
<h3>5.4.1 类比：FFT像"批量处理"，同时计算多项式在多个点的值</h3>
<p>假设你是一名质检员，需要检查一批产品在 \$n\$ 个不同测试条件下的性能。朴素的做法是逐个测试——每次重新设置设备、重新测量，总耗时 \$O(n^2)\$。FFT则像一台<strong>并行测试流水线</strong>：它利用测试条件之间的数学关联，同时完成所有测试，将总耗时压缩到 \$O(n \\log n)\$。</p>
<p>在多项式世界中，"测试条件"就是<strong>单位根</strong>（roots of unity）——复数域上是 \$e^{2\\pi i / n}\$，有限域上是满足 \$\\omega^n = 1\$ 的元素[^201^]。</p>
<h3>5.4.2 从DFT到FFT：\$O(n^2) \\rightarrow O(n \\log n)\$ 的算法奇迹</h3>
<p><strong>离散傅里叶变换（DFT）</strong>的定义非常直接：给定多项式的系数表示 \$[a_0, a_1, \\ldots, a_{n-1}]\$，求它在 \$n\$ 个单位根处的值：</p>
<p>\$\$y_k = \\sum_{j=0}^{n-1} a_j \\cdot \\omega^{jk}, \\quad k = 0, 1, \\ldots, n-1\$\$</p>
<p>其中 \$\\omega = e^{2\\pi i / n}\$ 是 \$n\$ 次本原单位根。</p>
<p>朴素实现需要对每个 \$k\$ 都做一次 \$n\$ 项求和——总复杂度 \$O(n^2)\$。 Cooley-Tukey FFT 的核心洞察是<strong>分治</strong>：</p>
<ul><li>将多项式拆分为<strong>偶次项</strong>和<strong>奇次项</strong>：</li>
</ul>   \$\$p(x) = p_{\\text{even}}(x^2) + x \\cdot p_{\\text{odd}}(x^2)\$\$
<ul><li>如果我们在 \$\\omega^0, \\omega^1, \\ldots, \\omega^{n-1}\$ 处求值，注意到 \$\\omega^{k + n/2} = -\\omega^k\$（单位根的对称性），所以：</li>
</ul>   \$\$p(\\omega^{k + n/2}) = p_{\\text{even}}(\\omega^{2k}) - \\omega^k \\cdot p_{\\text{odd}}(\\omega^{2k})\$\$
<ul><li>子问题规模减半，递归求解[^266^]</li>
</ul><h3>5.4.3 蝶形运算：分治策略的可视化</h3>
<pre class="code-block"><code>FFT蝶形运算（Cooley-Tukey）：n=8 的分治合并过程

第0层（输入）：系数 [a₀, a₁, a₂, a₃, a₄, a₅, a₆, a₇]

第1层（2点DFT）：
    ┌──────────┐    ┌──────────┐
    │ a₀ ──┐   │    │ a₄ ──┐   │
    │ a₄ ──┼──►│    │ a₆ ──┼──►│
    │      │   │    │      │   │
    │ a₂ ──┐   │    │ a₅ ──┐   │
    │ a₆ ──┼──►│    │ a₇ ──┼──►│
    └──────────┘    └──────────┘
    偶位↔奇位配对（按二进制逆序重排后）

    每个"蝶形"节点:
         ┌─────┐
    A ──►│  +  │──► A + ωᵏ·B
         │  ωᵏ│
    B ──►│  ·  │──► A - ωᵏ·B  ← 利用 ω^(k+n/2) = -ωᵏ
         └─────┘

第2层（4点DFT）：合并相邻的2点结果
         ┌──────────────┐
    E₀ ──┐              │
    E₁ ──┼──► 蝶形 ────►│
         │   (ω⁰, ω¹)   │
    O₀ ──┤              │
    O₁ ──┘              │
         └──────────────┘

第3层（8点DFT）：最终合并为完整结果
         
    ┌─────────────────────────────────────────────┐
    │  [y₀, y₁, y₂, y₃, y₄, y₅, y₆, y₇]          │
    │   = p(ω⁰), p(ω¹), ..., p(ω⁷)              │
    │   多项式在8个单位根处的求值结果             │
    └─────────────────────────────────────────────┘

总复杂度：每一层 O(n) 操作，共 log₂(n) 层 → O(n log n)
</code></pre>
<p>蝶形运算的名字来源于其连线图看起来像蝴蝶翅膀——两条输入线进入一对加减运算，输出两条结果线。每一层蝶形运算都利用了单位根的对称性 \$\\omega^{k+n/2} = -\\omega^k\$，使得同一对子结果能被复用。</p>
<h3>5.4.4 数论变换（NTT）：FFT在有限域上的版本</h3>
<p>ZK中的所有运算都发生在有限域 \$F_p\$ 上，而非复数域。复数域上的FFT不能直接搬过来——我们需要 \$F_p\$ 上的<strong>单位根</strong>。</p>
<p><strong>关键条件</strong>：素数 \$p\$ 需要满足 \$p - 1\$ 能被 \$2^L\$ 整除（\$L\$ 足够大），这样 \$F_p\$ 中才存在 \$2^L\$ 次本原单位根 \$\\omega\$。SNARK友好曲线（如BN254、BLS12-381）的标量域（scalar field）被专门设计来满足这个条件[^201^]。</p>
<p>NTT与复数FFT的区别仅在于：<br><ul><li>运算在 \$F_p\$ 中进行，所有加减乘都带模 \$p\$</li><br><li>单位根 \$\\omega\$ 是 \$F_p\$ 中满足 \$\\omega^{n} = 1\$ 的元素</li><br><li>无需处理浮点数精度问题——有限域运算是精确的</li></ul></p>
<p>Groth16的证明生成、PLONK中的quotient polynomial计算、STARK中的低度扩展，都重度依赖NTT来在系数表示和点值表示之间快速转换[^219^]。</p>
<hr>
<h2>5.5 多项式承诺的直觉</h2>
<h3>5.5.1 承诺是什么：先"封印"，后"揭示"</h3>
<p>多项式承诺（Polynomial Commitment）是ZK系统的"动力引擎"[^96^]。它的工作流程可以类比为：</p>
<blockquote><strong>密封的试卷</strong>：你（证明者）把答案写在一份试卷上，放进密封袋交给验证者。之后验证者随机抽取一道题，你只展示那一题的答案和解题过程。验证者可以确认整份试卷是你写的，但只知道这一题的答案[^243^]。</blockquote>
<p>形式化地说，承诺方案有三个算法：<br><ul><li><strong>Commit</strong>（承诺）：将多项式 \$p(x)\$ "封印"为一个短字符串（通常是椭圆曲线点）</li><br><li><strong>Open</strong>（开启）：在验证者指定的点 \$z\$ 处，揭示 \$p(z)\$ 的值，并附上一个<strong>证明</strong></li><br><li><strong>Verify</strong>（验证）：验证者检查"承诺 + 点值 + 证明"三者是否一致</li></ul></p>
<h3>5.5.2 KZG承诺的直觉：用椭圆曲线配对"压缩"多项式</h3>
<p>Kate-Zaverucha-Goldberg（KZG）承诺是SNARK中最常用的多项式承诺方案。它的核心直觉可以用以下ASCII图表示：</p>
<pre class="code-block"><code>KZG多项式承诺直觉："在秘密点 τ 处求值多项式"

可信设置阶段（一次性，多方参与）:
    ┌─────────────────────────────────────────┐
    │  生成公共参考串 SRS = {g, g^τ, g^(τ²), ..., g^(τᵈ)} │
    │  τ 是秘密随机数，设置完成后必须销毁     │
    │  g 是椭圆曲线群 G₁ 的生成元              │
    └─────────────────────────────────────────┘
                      │
                      ▼
承诺阶段（证明者）:
    ┌─────────────────────────────────────────┐
    │  p(x) = a₀ + a₁x + a₂x² + ... + a_dxᵈ   │
    │                                         │
    │  Commit(p) = g^(p(τ))                   │
    │            = g^(a₀ + a₁τ + a₂τ² + ...)    │
    │            = Π (g^(τⁱ))^(aᵢ)             │
    │  ← 用SRS中的点做线性组合，无需知道 τ     │
    └─────────────────────────────────────────┘
                      │
                      ▼  发送给验证者
                      
开启阶段（验证者随机选点 z）:
    ┌─────────────────────────────────────────┐
    │  证明者计算: y = p(z)                   │
    │  证明者构造证明 π（证明 p(τ)-p(z) 被 τ-z 整除）│
    └─────────────────────────────────────────┘
                      │
                      ▼
验证阶段（验证者，仅需2次配对）:
    ┌─────────────────────────────────────────┐
    │  e(Commit(p) / g^y,  h) =? e(π,  h^(τ-z)/g?) │
    │                                         │
    │  配对等式验证:                           │
    │  e(g^(p(τ)-p(z)),  h) = e(g^Q(τ),  h^(τ-z))  │
    │                                         │
    │  ← 利用配对的双线性性质 e(g^a, h^b)=e(g,h)^(ab) │
    │    验证"p(τ)-p(z) = Q(τ)·(τ-z)" 这一关系    │
    └─────────────────────────────────────────┘
</code></pre>
<p>KZG承诺的验证完全依赖配对（Pairing）：验证者无需知道秘密参数 \$\\tau\$，仅凭公共参考串和配对等式就能确认多项式求值的正确性[^190^][^194^]。</p>
<p>这里的关键是Schwartz-Zippel引理：如果证明者造假——即声称 \$p(z) = y'\$ 但真实值为 \$y \\neq y'\$——那么多项式 \$p(x) - y'\$ 和真实多项式 \$p(x) - y\$ 是不同的。在随机点 \$\\tau\$ 处恰好碰撞的概率极低[^194^]。</p>
<hr>
<h2>5.6 实战：Python多项式运算</h2>
<h3>5.6.1 多项式加减乘与拉格朗日插值</h3>
<p>下面的Python代码展示了如何在有限域上进行多项式运算，并实现拉格朗日插值。所有运算都在模 \$p\$ 下进行，模拟ZK中的实际场景。</p>
<pre class="code-block"><code>"""
有限域上的多项式运算与拉格朗日插值
目标：帮助ZK开发者建立多项式操作的代码直觉
"""

# 使用一个小的素数域便于手工验证；生产环境用 256-bit 大素数
P = 17  # 有限域 F_17

def mod(a):
    """将整数映射到 F_p"""
    return ((a % P) + P) % P

def mod_inv(a):
    """模逆元：用费马小定理，a^(p-2) ≡ a^(-1) (mod p)"""
    return pow(mod(a), P - 2, P)

def poly_add(a, b):
    """多项式加法：系数向量逐位相加"""
    n = max(len(a), len(b))
    res = [0] * n
    for i in range(n):
        av = a[i] if i < len(a) else 0
        bv = b[i] if i < len(b) else 0
        res[i] = mod(av + bv)
    # 去掉末尾的零系数（trim）
    while len(res) > 1 and res[-1] == 0:
        res.pop()
    return res

def poly_mul(a, b):
    """多项式乘法：朴素卷积 O(n²)
    在ZK生产环境中，这步会用NTT加速到 O(n log n)"""
    if not a or not b:
        return [0]
    res = [0] * (len(a) + len(b) - 1)
    for i in range(len(a)):
        for j in range(len(b)):
            res[i + j] = mod(res[i + j] + a[i] * b[j])
    # trim
    while len(res) > 1 and res[-1] == 0:
        res.pop()
    return res

def lagrange_interpolation(points):
    """
    拉格朗日插值：从点集 [(x₀,y₀), (x₁,y₁), ...] 重构多项式
    返回系数向量 [a₀, a₁, ..., a_d]
    """
    n = len(points)
    xs = [mod(p[0]) for p in points]
    ys = [mod(p[1]) for p in points]
    
    # 从0多项式开始累加
    result = [0]
    
    for i in range(n):
        # 构造第 i 个基多项式 L_i(x) = Π_{j≠i} (x - x_j) / (x_i - x_j)
        # 先构造分子：Π_{j≠i} (x - x_j)
        numerator = [1]  # 从常数1开始
        denominator = 1
        
        for j in range(n):
            if i == j:
                continue
            # 乘上 (x - x_j)：系数表示为 [-x_j, 1]
            term = [mod(-xs[j]), 1]
            numerator = poly_mul(numerator, term)
            # 分母累乘 (x_i - x_j)
            denominator = mod(denominator * mod(xs[i] - xs[j]))
        
        # 将基多项式乘以 y_i / denominator
        inv_denom = mod_inv(denominator)
        scalar = mod(ys[i] * inv_denom)
        scaled = [mod(c * scalar) for c in numerator]
        
        # 累加到结果
        result = poly_add(result, scaled)
    
    return result

def eval_poly(coeffs, x):
    """用霍纳法则（Horner's method）求多项式值"""
    x = mod(x)
    res = 0
    for i in range(len(coeffs) - 1, -1, -1):
        res = mod(res * x + coeffs[i])
    return res


# ======== 演示 ========
if __name__ == "__main__":
    print(f"在有限域 F_{P} 上运行多项式运算\\\\n")
    
    # 1. 多项式乘法演示
    p1 = [2, 1]      # 2 + x
    p2 = [3, 0, 1]   # 3 + x²
    p_prod = poly_mul(p1, p2)
    print(f"({p1}) × ({p2}) = {p_prod}")
    # 验证：(2+x)(3+x²) = 6 + 3x + 2x² + x³ → mod 17 不变（系数均<17）
    
    # 2. 拉格朗日插值演示
    # 给定点：(1,5), (2,3), (3,10) — 应有唯一二次多项式穿过它们
    points = [(1, 5), (2, 3), (3, 10)]
    poly = lagrange_interpolation(points)
    print(f"\\\\n给定点 {points}")
    print(f"插值多项式系数: {poly}")
    
    # 验证：在原始点上是否恢复正确值
    for x, y in points:
        val = eval_poly(poly, x)
        print(f"  p({x}) = {val}, 期望 {y} {'✓' if val == mod(y) else '✗'}")
    
    # 3. 展示"唯一性"：用4个点确定三次多项式
    points4 = [(0, 1), (1, 2), (2, 5), (3, 10)]
    poly4 = lagrange_interpolation(points4)
    print(f"\\\\n4个点 {points4} → 三次多项式系数: {poly4}")
</code></pre>
<p>运行这段代码，你会看到有限域上的多项式乘法和插值结果。关键在于：在有限域上，所有系数运算都被"锁"在 \$0\$ 到 \$p-1\$ 的范围内——这和ZK生产环境中的运算规则完全一致，只是规模小得多[^274^]。</p>
<h3>5.6.2 用NumPy实现FFT与NTT对比</h3>
<p>下面的代码展示复数域FFT和有限域NTT的对比，帮助理解两者在结构上的相似性。</p>
<pre class="code-block"><code>"""
FFT（复数域）与NTT（有限域）对比演示
核心洞察：两者都是"在单位根处评估多项式"，只是单位根所在的域不同
"""
import numpy as np

# ========== 第一部分：复数域FFT（NumPy原生支持）==========
print("=" * 50)
print("【复数域FFT】在单位圆上评估多项式")
print("=" * 50)

# 多项式 p(x) = 1 + 2x + 3x² + 4x³
coeffs = [1, 2, 3, 4]
print(f"多项式系数: {coeffs}")

# np.fft.fft 自动处理2的幂次补零
fft_result = np.fft.fft(coeffs)
print(f"FFT结果（在单位根处的值）:")
for i, val in enumerate(fft_result):
    # 提取实部便于阅读（小虚数是浮点误差）
    real = round(val.real, 6)
    imag = round(val.imag, 6)
    print(f"  p(ω^{i}) ≈ {real} + {imag}j")

# 逆FFT恢复系数
ifft_result = np.fft.ifft(fft_result)
print(f"逆FFT恢复系数: {[round(x.real, 6) for x in ifft_result]}")


# ========== 第二部分：有限域NTT（手动实现）==========
print("\\\\n" + "=" * 50)
print("【有限域NTT】在 F_17 上评估多项式")
print("=" * 50)

P_ntt = 17  # 素数模

# 检查 2-adic 子群：17-1 = 16 = 2^4，所以存在16次单位根
# 本原根 g = 3 是 F_17 的乘法生成元
# 16次本原单位根 ω = g^((p-1)/16) = 3^1 = 3
# 验证：3^16 mod 17 = 1 ✓
g = 3
omega = pow(g, (P_ntt - 1) // 16, P_ntt)
print(f"F_{P_ntt} 的16次本原单位根 ω = {omega}")
print(f"验证 ω^16 mod {P_ntt} = {pow(omega, 16, P_ntt)}")

def ntt(a, omega, P):
    """
    迭代式NTT实现（Cooley-Tukey）
    a: 系数向量（长度必须是2的幂）
    omega: n次本原单位根
    P: 素数模
    """
    n = len(a)
    # 位逆序重排（bit-reversal permutation）
    j = 0
    for i in range(1, n):
        bit = n >> 1
        while j & bit:
            j ^= bit
            bit >>= 1
        j ^= bit
        if i < j:
            a[i], a[j] = a[j], a[i]
    
    # 蝶形运算，逐层合并
    length = 2
    while length <= n:
        # 当前层的单位根: ω^(n/length)
        wlen = pow(omega, n // length, P)
        for i in range(0, n, length):
            w = 1
            half = length // 2
            for j in range(i, i + half):
                # 蝶形: (u, v) → (u + w·v, u - w·v)
                u = a[j]
                v = a[j + half] * w % P
                a[j] = (u + v) % P
                a[j + half] = (u - v + P) % P  # +P 确保非负
                w = w * wlen % P
        length <<= 1
    return a

def intt(a, omega, P):
    """逆NTT：将点值转换回系数"""
    n = len(a)
    # NTT的单位根取逆
    omega_inv = pow(omega, P - 2, P)  # 费马小定理求逆
    a = ntt(a.copy(), omega_inv, P)
    n_inv = pow(n, P - 2, P)
    return [(x * n_inv) % P for x in a]

# 运行NTT（需要补零到2的幂）
coeffs_ntt = [1, 2, 3, 4]  # 同之前的多项式
n = 8  # 补零到8点（必须 ≤ 16，因为16次单位根存在）
coeffs_padded = coeffs_ntt + [0] * (n - len(coeffs_ntt))

print(f"\\\\n补零后的系数（长度={n}）: {coeffs_padded}")
ntt_result = ntt(coeffs_padded.copy(), pow(omega, 16 // n, P_ntt), P_ntt)
print(f"NTT结果（在F_{P_ntt}单位根处的值）:")
for i, val in enumerate(ntt_result):
    print(f"  p(ω^{i}) = {val}")

# 逆NTT恢复系数
recovered = intt(ntt_result, pow(omega, 16 // n, P_ntt), P_ntt)
print(f"逆NTT恢复系数: {recovered}")


print("\\\\n" + "=" * 50)
print("核心对比总结:")
print("=" * 50)
print("| 特性        | FFT（复数域）         | NTT（有限域）          |")
print("|------------|---------------------|------------------------|")
print("| 单位根      | e^(2πi/n)           | ω ∈ F_p, ω^n = 1       |")
print("| 运算        | 浮点复数运算         | 模p整数运算（精确无误差）|")
print("| 精度问题    | 浮点舍入误差         | 无（有限域是离散的）    |")
print("| ZK中的应用  | 无（不适用于密码学） | Groth16/PLONK/STARK核心 |")
</code></pre>
<p>运行这段代码，你会观察到两个关键现象：</p>
<ul><li><strong>复数FFT</strong>的结果包含微小虚数分量（浮点误差），而<strong>NTT</strong>的结果完全是精确的整数——这正是有限域运算的魅力[^274^]。</li>
<li><strong>NTT的逆变换完美恢复原始系数</strong>（忽略补零），验证了算法正确性。</li>
</ul><p>在生产级ZK库（如 arkworks、@noble/curves）中，NTT被重度优化：使用 Montgomery 乘法、并行蝶形运算、甚至GPU加速[^228^]。但对于理解原理而言，上面的几十行代码已经揭示了核心结构。</p>
<hr>
<h2>5.7 本章小结</h2>
<p>本章建立了ZK证明系统的"核心语言"——多项式。关键概念回顾：</p>
<ul><li><strong>多项式是ZK的通用语言</strong>：无论Groth16、PLONK还是STARK，都将计算约束编码为多项式等式。多项式的代数结构（可加减乘除、可因式分解、根的唯一性）为ZK提供了数学基础。</li>
<li><strong>拉格朗日插值是"点→多项式"的桥梁</strong>：给定 \$n+1\$ 个点，存在唯一的次数不超过 \$n\$ 的多项式穿过它们。基多项式 \$L_i(x)\$ 的构造让每个点"只在自己的位置上起作用"，最终多项式是所有基的加权叠加。在ZK中，QAP和PLONK都用插值将离散的电路约束转化为连续的多项式对象[^64^][^27^]。</li>
<li><strong>FFT/NTT将多项式运算从 \$O(n^2)\$ 加速到 \$O(n \\log n)\$</strong>：Cooley-Tukey分治策略利用单位根的对称性，通过蝶形运算逐层合并子问题。在有限域 \$F_p\$ 上的版本称为NTT，需要 \$p-1\$ 能被 \$2^L\$ 整除以保证单位根存在——这正是SNARK友好曲线的设计约束[^201^]。</li>
<li><strong>多项式承诺 = "封印多项式，后揭示单点"</strong>：KZG承诺通过在秘密点 \$\\tau\$ 处求值多项式来"压缩"它，验证时利用配对等式确认求值的正确性，而无需暴露 \$\\tau\$ 或全部系数[^190^][^194^]。</li>
</ul><hr>
<h2>5.8 思考题与练习</h2>
<h3>概念理解</h3>
<ul><li><strong>唯一性证明</strong>：给定有限域 \$F_7\$ 上的三个点 \$(0, 2), (1, 5), (2, 3)\$，用拉格朗日插值手动计算（可用纸笔或Python）穿过它们的多项式 \$p(x)\$，并验证 \$p(0)=2, p(1)=5, p(2)=3\$。如果额外增加第四个点 \$(3, y_3)\$，插值多项式的次数会发生什么变化？</li>
<li><strong>Schwartz-Zippel引理的应用</strong>：假设某个ZK系统使用 \$F_p\$ 上的100次多项式，其中 \$p\$ 是一个256位素数（约 \$10^{77}\$）。如果验证者随机选取一个点检查，证明者伪造多项式恰好通过检查的概率是多少？请用数量级估算说明为什么"单点验证"在实践中足够安全。</li>
</ul><h3>动手实践</h3>
<ul><li><strong>NTT多项式乘法</strong>：扩展本章的NTT代码，实现两个多项式的快速乘法。步骤提示：(a) 将两个多项式补零到长度 \$2d+1\$（\$d\$ 为较高次数）；(b) 分别做NTT得到点值表示；(c) 逐点相乘；(d) 逆NTT转回系数表示。在 \$F_{17}\$ 上验证 \$(1+2x+3x^2) \\times (2+x+4x^2)\$ 的结果。</li>
<li><strong>vanishing polynomial 构造</strong>：在 \$F_{17}\$ 上构造多项式 \$Z(x) = (x-1)(x-2)(x-3)(x-4)\$，计算它的系数表示。然后任选一点 \$z \\notin \\{1,2,3,4\\}\$，验证 \$Z(z) \\neq 0\$。这个多项式在ZK中被称为"vanishing polynomial"——它在所有约束点处为零，在其他地方非零。理解它的构造是理解QAP和PLONK约束验证的关键[^108^]。</li></ul>`;
---

<Layout title="第05章 - 数学基础IV：多项式、插值与FFT" description="ZK学习第05章：数学基础IV：多项式、插值与FFT">
  <main class="min-h-screen bg-[#0A0A0F]">
    <section class="py-12 px-4 border-b border-[#2a2a3a]">
      <div class="max-w-4xl mx-auto">
        <a href={`${baseUrl}zk-learning/`} class="inline-flex items-center gap-2 text-sm text-[#6a6a80] hover:text-[#00d4aa] transition-colors mb-6">
          <span>←</span> 返回 ZK 学习列表
        </a>
        <div class="flex items-center gap-3 mb-4">
          <div class="w-10 h-10 rounded-full flex items-center justify-center text-sm font-bold text-[#00d4aa] bg-[rgba(0,212,170,0.1)] border border-[rgba(0,212,170,0.2)]">
            05
          </div>
          <span class="px-2.5 py-1 text-xs rounded-full bg-[rgba(0,212,170,0.08)] text-[#00d4aa] border border-[rgba(0,212,170,0.15)]">
            数学基础
          </span>
        </div>
        <h1 class="text-3xl md:text-4xl font-bold text-white">数学基础IV：多项式、插值与FFT</h1>
      </div>
    </section>

    <section class="py-8 px-4">
      <div class="max-w-4xl mx-auto">
        <article class="prose-zk" set:html={htmlContent} />
      </div>
    </section>

    <section class="py-8 px-4 border-t border-[#2a2a3a]">
      <div class="max-w-4xl mx-auto flex justify-between items-center">
        
          <a href={`${baseUrl}zk-learning/04/`} class="flex items-center gap-2 px-4 py-2 rounded-lg border border-[#2a2a3a] bg-[#16161e] text-[#9a9ab0] hover:text-white hover:border-[#3a3a4a] transition-all">
            <span>←</span>
            <div class="text-left">
              <div class="text-xs text-[#6a6a80]">上一章</div>
              <div class="text-sm font-medium">数学基础III：椭圆曲线与双线性配对</div>
            </div>
          </a>
        
          <a href={`${baseUrl}zk-learning/06/`} class="flex items-center gap-2 px-4 py-2 rounded-lg border border-[#2a2a3a] bg-[#16161e] text-[#9a9ab0] hover:text-white hover:border-[#3a3a4a] transition-all">
            <div class="text-right">
              <div class="text-xs text-[#6a6a80]">下一章</div>
              <div class="text-sm font-medium">电路基础：从程序到约束系统</div>
            </div>
            <span>→</span>
          </a>
      </div>
    </section>
  </main>
</Layout>

<style is:global>
  .prose-zk { color: #9a9ab0; line-height: 1.8; font-size: 0.95rem; }
  .prose-zk h1 { color: #fff; font-size: 1.8rem; font-weight: 700; margin: 2rem 0 1rem; padding-bottom: 0.5rem; border-bottom: 1px solid #2a2a3a; }
  .prose-zk h2 { color: #fff; font-size: 1.4rem; font-weight: 600; margin: 2rem 0 0.75rem; padding-bottom: 0.4rem; border-bottom: 1px solid #2a2a3a; }
  .prose-zk h3 { color: #e0e0f0; font-size: 1.15rem; font-weight: 600; margin: 1.5rem 0 0.5rem; }
  .prose-zk h4 { color: #e0e0f0; font-size: 1.05rem; font-weight: 600; margin: 1.25rem 0 0.5rem; }
  .prose-zk h5, .prose-zk h6 { color: #c0c0d0; font-size: 1rem; font-weight: 600; margin: 1rem 0 0.5rem; }
  .prose-zk p { margin: 0.75rem 0; }
  .prose-zk ul { margin: 0.75rem 0; padding-left: 1.5rem; }
  .prose-zk li { margin: 0.35rem 0; }
  .prose-zk li::marker { color: #00d4aa; }
  .prose-zk blockquote { margin: 1rem 0; padding: 0.75rem 1rem; border-left: 3px solid rgba(0, 212, 170, 0.4); background: rgba(0, 212, 170, 0.03); border-radius: 0 6px 6px 0; color: #b0b0c0; font-style: italic; }
  .prose-zk hr { border: none; border-top: 1px solid #2a2a3a; margin: 1.5rem 0; }
  .prose-zk .code-block { background: #0f0f16; border: 1px solid #2a2a3a; border-radius: 8px; padding: 1rem; margin: 1rem 0; overflow-x: auto; font-family: 'JetBrains Mono', 'Fira Code', monospace; font-size: 0.85rem; line-height: 1.6; color: #c0c0d0; }
  .prose-zk .inline-code { background: rgba(0, 212, 170, 0.08); color: #00d4aa; padding: 0.15rem 0.4rem; border-radius: 4px; font-family: 'JetBrains Mono', 'Fira Code', monospace; font-size: 0.88em; border: 1px solid rgba(0, 212, 170, 0.15); }
  .prose-zk strong { color: #e0e0f0; font-weight: 600; }
  .prose-zk em { color: #b0b0c0; }
</style>
